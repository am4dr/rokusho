rootProject.name = 'rokusho'

include 'application'

def packages = ['javafx-commons', 'gui', 'core', 'app-gui', 'launcher']
packages.each {
    include it
    project(':'+it).projectDir = new File(rootProject.projectDir, "packages/$it")
}
